% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand_shiny.R
\name{create_brand_shiny_app}
\alias{create_brand_shiny_app}
\title{Set Up a Branded Shiny App}
\usage{
create_brand_shiny_app(path = ".", overwrite = FALSE)
}
\arguments{
\item{path}{Project directory. Defaults to the current working
directory. Created if it doesn't exist.}

\item{overwrite}{Logical. Overwrite existing files? Default \code{FALSE}.}
}
\value{
Invisibly returns a character vector of copied file paths.
}
\description{
Scaffolds a minimal single-file Shiny app built on
\code{\link[=brand_page_sidebar]{brand_page_sidebar()}} — a sidebar of inputs, a plot, a palette
preview, and a summary panel, with no theming code in it. Use this as
the starting point for a new branded app.
}
\details{
For a KPI dashboard on \code{\link[=brand_page_navbar]{brand_page_navbar()}}, see
\code{\link[=create_brand_shiny_dashboard]{create_brand_shiny_dashboard()}}. For a leaflet/plotly geospatial app
on \code{\link[=brand_page_fluid]{brand_page_fluid()}}, see \code{\link[=create_brand_shiny_map]{create_brand_shiny_map()}}.

This function copies the following into \code{path}:
\describe{
\item{\verb{_brand.yml}}{From the brandkit cache, in the bslib format
(see \emph{Brand format} below).}
\item{\code{app.R}}{The app template.}
}
Local font files and logo files referenced by the brand are copied
alongside, preserving their relative paths.

Unlike the Quarto scaffolds, nothing else needs copying: the
\verb{brand_page_*()} wrappers pull brandkit's widget CSS overrides,
dark-mode CSS, and the dark-mode toggle in at runtime, so there is no
stylesheet to place in the project.
}
\section{Brand format}{

This is the mirror image of what \code{\link[=create_brand_quarto_html]{create_brand_quarto_html()}} and
friends do. Quarto 1.8 wants light/dark colours nested under each
colour key (\verb{primary: \{light: ..., dark: ...\}}); bslib rejects that
outright — \code{bslib::bs_theme(brand = )} errors with "\code{color.primary}
must be a single string or \code{NULL}, not a list" — and wants a flat
\verb{color:} section with dark values in a separate \verb{color-dark:} section.

So if \code{path} already holds a \verb{_brand.yml} in the Quarto format, it is
converted to the bslib format regardless of \code{overwrite}, since a
leftover Quarto-format file always needs fixing before the app will
start. An already-bslib-compatible \verb{_brand.yml} is left alone unless
\code{overwrite = TRUE}.

Bootstrap variables (border radius and so on) are written under
\verb{defaults: bootstrap: defaults:} rather than the \verb{theme:} key that
\code{\link[=configure_brand]{configure_brand()}} writes. Both are read back by brandkit itself, but
only the former is read by bslib — a \verb{theme:} section is silently
ignored by \code{bs_theme(brand = )}, so a border radius configured there
never reaches the app.

If the cached brand references a logo whose file can't actually be
found (e.g. a stale cache pointing at a different project), the
\verb{logo:} section is omitted from the written \verb{_brand.yml} rather than
pointing at a file that will never be copied.
}

\examples{
\dontrun{
create_brand_shiny_app(path = "my-app")
shiny::runApp("my-app")
}

}
