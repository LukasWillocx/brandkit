% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand_quarto.R
\name{create_brand_quarto_slides}
\alias{create_brand_quarto_slides}
\title{Set Up a Quarto Revealjs Slides Project}
\usage{
create_brand_quarto_slides(path = ".", examples = TRUE, overwrite = FALSE)
}
\arguments{
\item{path}{Project directory. Defaults to the current working directory.}

\item{examples}{Logical. Copy the example \code{slides.qmd}? Default \code{TRUE}.}

\item{overwrite}{Logical. Overwrite existing files? Default \code{FALSE}.}
}
\value{
Invisibly returns a character vector of copied file paths.
}
\description{
Copies \verb{_brand.yml}, custom SCSS overrides, and an example revealjs
presentation into a Quarto project directory. After running this,
Quarto auto-detects \verb{_brand.yml} and applies it to the revealjs
format. The SCSS file layers additional polish (logo sizing, nav
pills, scrollbars) on top.
}
\details{
This function copies the following into \code{path}:
\describe{
\item{\verb{_brand.yml}}{From the brandkit cache (your configured brand).}
\item{\code{brandkit.scss}}{Custom SCSS overrides — layered after brand in
the Quarto theme.}
\item{\code{slides.qmd}}{Sample revealjs presentation (if \code{examples = TRUE}).
Its title slide background is written as a literal hex colour —
the brand's primary colour darkened — computed at copy time so
the deck's white title/subtitle/author/date text stays legible
regardless of the brand's actual primary colour.}
}

In your \code{.qmd} YAML header, reference the SCSS like this:

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{format:
  revealjs:
    theme: [brand, brandkit.scss]
    logo: medium
}\if{html}{\out{</div>}}

For dark mode slides, add \code{brand-mode: dark} and call
\code{brand_quarto_setup("dark")} in the setup chunk. For plotly in slides,
always pass fixed dimensions: \code{brand_plotly(p, width = 1000, height = 600)}.

If \code{path} already has a \verb{_brand.yml} written by \code{\link[=configure_brand]{configure_brand()}}
(the Shiny/bslib format, with \verb{color-dark:}/\verb{theme:} keys), it is
converted to the Quarto-compatible format regardless of \code{overwrite} —
Quarto's own renderer rejects those bslib-only keys outright, so a
leftover bslib-format file always needs fixing. An already
Quarto-compatible \verb{_brand.yml} is left alone unless \code{overwrite = TRUE}.

If the cached brand references a logo whose file can't actually be
found (e.g. a stale cache pointing at a different project), the
\verb{logo:} section is omitted from the written \verb{_brand.yml} rather than
pointing at a file that will never be copied.
}
