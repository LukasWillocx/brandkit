% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand_quarto.R
\name{create_brand_quarto_dashboard}
\alias{create_brand_quarto_dashboard}
\title{Set Up a Quarto Shiny Dashboard Project}
\usage{
create_brand_quarto_dashboard(path = ".", examples = TRUE, overwrite = FALSE)
}
\arguments{
\item{path}{Project directory. Defaults to the current working
directory. Created if it doesn't exist.}

\item{examples}{Logical. Copy the example \code{dashboard.qmd}? Default
\code{TRUE}.}

\item{overwrite}{Logical. Overwrite existing files? Default \code{FALSE}.}
}
\value{
Invisibly returns a character vector of copied file paths.
}
\description{
Copies \verb{_brand.yml}, the brandkit SCSS overrides, and an example
\code{dashboard.qmd} into a Quarto project directory, configured to render
to Quarto's \code{dashboard} format with a Shiny runtime. The result is a
KPI dashboard — a row of value boxes over filtered charts and a table
— driven by sidebar inputs.
}
\details{
This is the Quarto counterpart to \code{\link[=create_brand_shiny_dashboard]{create_brand_shiny_dashboard()}}.
The two produce a similar layout by different routes: this one lays
the dashboard out in Quarto markdown and takes its ggplot2 theming
from a single \code{\link[=brand_quarto_setup]{brand_quarto_setup()}} call, while the Shiny version
builds the same structure in \code{bslib} and takes its theming from
\code{\link[=brand_page_navbar]{brand_page_navbar()}}. Prefer this one when the dashboard sits
alongside other Quarto documents; prefer the Shiny one when it needs
to grow into a full application.

This function copies the following into \code{path}:
\describe{
\item{\verb{_brand.yml}}{From the brandkit cache (your configured brand),
in the Quarto-compatible format.}
\item{\code{brandkit.scss}}{The same SCSS overrides the HTML report
scaffold uses — layered after brand in the theme. Its title-banner
rules are inert in a dashboard (there is no banner div to match),
but its card, table, scrollbar, and nav styling all apply.}
\item{\code{dashboard.qmd}}{Sample Shiny dashboard (if \code{examples = TRUE}).}
}

Because the document declares \code{server: shiny}, it is served rather
than rendered to a static file:

\if{html}{\out{<div class="sourceCode">}}\preformatted{quarto serve dashboard.qmd
}\if{html}{\out{</div>}}

Rendering it with \verb{quarto render} produces the supporting files but
not a runnable page — that needs a Shiny-capable server. This requires
Quarto >= 1.4 (the \code{dashboard} format) and the \pkg{shiny} package.

If \code{path} already has a \verb{_brand.yml} in the Shiny/bslib format (with
\verb{color-dark:}/\verb{theme:} keys — what \code{\link[=configure_brand]{configure_brand()}} writes unless
its output-format toggle is set to Quarto), it is converted to the
Quarto-compatible format regardless of \code{overwrite} — Quarto's own
renderer rejects those bslib-only keys outright, so a leftover
bslib-format file always needs fixing. An already Quarto-compatible
\verb{_brand.yml} is left alone unless \code{overwrite = TRUE}.

If the cached brand references a logo whose file can't actually be
found (e.g. a stale cache pointing at a different project), the
\verb{logo:} section is omitted from the written \verb{_brand.yml} rather than
pointing at a file that will never be copied.
}
\examples{
\dontrun{
create_brand_quarto_dashboard(path = "my-dashboard")
# then, in a terminal:  quarto serve my-dashboard/dashboard.qmd
}

}
