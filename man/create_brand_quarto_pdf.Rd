% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand_quarto.R
\name{create_brand_quarto_pdf}
\alias{create_brand_quarto_pdf}
\title{Set Up a Quarto Typst PDF Project}
\usage{
create_brand_quarto_pdf(path = ".", examples = TRUE, overwrite = FALSE)
}
\arguments{
\item{path}{Project directory. Defaults to the current working directory.}

\item{examples}{Logical. Copy an example \code{.qmd} report? Default \code{TRUE}.}

\item{overwrite}{Logical. Overwrite existing files? Default \code{FALSE}.}
}
\value{
Invisibly returns a character vector of copied file paths.
}
\description{
Copies \verb{_brand.yml}, a branded Typst format extension, and an example
report into a Quarto project directory, configured to render to PDF
via Quarto's Typst engine. Quarto's own \verb{_brand.yml} integration
already applies brand colours and fonts to headings, links, and body
text in Typst output — but not to code, so the \verb{_extensions/brandkit/}
extension this function installs closes that gap: both inline code
spans and fenced code blocks pick up the brand's configured monospace
font, sized to match the surrounding body text (Typst has no
root-relative unit equivalent to CSS \code{rem}, so this is resolved to an
absolute length rather than left to compound), and inline code spans
additionally get a colour of their own — a blend of the brand's
secondary accent and foreground colour — so they stand out in running
text. The extension also layers a full-bleed title banner on top — a
diagonal-stripe field (Linux Mint wallpaper style, drawn as Typst
polygons, no image asset) that runs primary-coloured behind the
title/subtitle and switches to secondary-coloured stripes past an
angled seam — plus coloured headings, a coloured footer, and a
code-block corner radius matching the brand's configured
border-radius, none of which the default Typst article template
provides on its own.
}
\details{
This function copies the following into \code{path}:
\describe{
\item{\verb{_brand.yml}}{From the brandkit cache (your configured brand).}
\item{\verb{_extensions/brandkit/}}{A Quarto Typst format extension
(\verb{_extension.yml} generated with a brand-derived code-block radius
and an absolute base font size; \code{template.typ}, \code{typst-template.typ},
\code{typst-show.typ}, \code{page.typ}, and Quarto's own supporting partials)
that adds a full-bleed diagonal-stripe title banner (with the logo,
if configured, placed inline in it), coloured headings, a coloured
footer, and brand-matched code styling (monospace font and body-
matched size for inline code and fenced blocks alike, plus a
secondary/foreground colour blend on inline code) on top of
Quarto's default Typst article template. When there's no title,
the banner is skipped and the logo falls back to a plain
page-corner mark instead.}
\item{\code{report-pdf.qmd}}{Sample Typst PDF report (if \code{examples = TRUE}).}
}

In your \code{.qmd} YAML header, use the extension's format:

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{format:
  brandkit-typst:
    toc: true
}\if{html}{\out{</div>}}

Render with \verb{quarto render report-pdf.qmd} to produce a PDF. Requires
Quarto >= 1.8 (brand.yml support for the Typst format); Quarto bundles
the Typst compiler itself, so no separate Typst installation is needed.

If \code{path} already has a \verb{_brand.yml} written by \code{\link[=configure_brand]{configure_brand()}}
(the Shiny/bslib format, with \verb{color-dark:}/\verb{theme:} keys), it is
converted to the Quarto-compatible format regardless of \code{overwrite} —
Quarto's own renderer rejects those bslib-only keys outright, so a
leftover bslib-format file always needs fixing. An already
Quarto-compatible \verb{_brand.yml} is left alone unless \code{overwrite = TRUE}.

If the cached brand references a logo whose file can't actually be
found (e.g. a stale cache pointing at a different project), the
\verb{logo:} section is omitted from the written \verb{_brand.yml} rather than
pointing at a file that will never be copied.
}
