% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand_shiny.R
\name{create_brand_shiny_dashboard}
\alias{create_brand_shiny_dashboard}
\title{Set Up a Branded Shiny Dashboard}
\usage{
create_brand_shiny_dashboard(path = ".", overwrite = FALSE)
}
\arguments{
\item{path}{Project directory. Defaults to the current working
directory. Created if it doesn't exist.}

\item{overwrite}{Logical. Overwrite existing files? Default \code{FALSE}.}
}
\value{
Invisibly returns a character vector of copied file paths.
}
\description{
Scaffolds a single-file KPI dashboard built on \code{\link[=brand_page_navbar]{brand_page_navbar()}}:
a row of \code{bslib::value_box()} tiles, revenue and product-mix charts, a
trends tab, and a DT data table — all driven by sample data you
replace with your own.
}
\details{
Copies \verb{_brand.yml} (bslib format), \code{app.R}, and any local font and
logo files, exactly as \code{\link[=create_brand_shiny_app]{create_brand_shiny_app()}} does — see its
documentation for how the brand file is written and converted.

The template uses the \pkg{DT} package for its data table. DT is a
suggested dependency of brandkit, so it may not be installed; this
function says so rather than leaving you to hit the error at app
start. DT tables are styled in both light and dark mode by brandkit's
CSS overrides, so the template passes no theming arguments to
\code{datatable()}.
}
\examples{
\dontrun{
create_brand_shiny_dashboard(path = "my-dashboard")
shiny::runApp("my-dashboard")
}

}
