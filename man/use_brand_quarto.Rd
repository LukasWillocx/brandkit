% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand_quarto.R
\name{use_brand_quarto}
\alias{use_brand_quarto}
\title{Set Up brandkit for a Quarto Project}
\usage{
use_brand_quarto(path = ".", examples = TRUE, overwrite = FALSE)
}
\arguments{
\item{path}{Project directory. Defaults to the current working directory.}

\item{examples}{Logical. Copy example \code{.qmd} files (report + slides)?
Default \code{TRUE}.}

\item{overwrite}{Logical. Overwrite existing files? Default \code{FALSE}.}
}
\value{
Invisibly returns a character vector of copied file paths.
}
\description{
Copies \verb{_brand.yml}, custom SCSS overrides, and optionally example
documents into a Quarto project directory. After running this, Quarto
auto-detects \verb{_brand.yml} and applies it to HTML, dashboard, revealjs,
and typst formats. The SCSS file layers additional polish on top.
}
\details{
This function copies the following into \code{path}:
\describe{
\item{\verb{_brand.yml}}{From the brandkit cache (your configured brand).}
\item{\code{brandkit.scss}}{Custom SCSS overrides for cards, tables,
scrollbars, and nav components — layered after brand in the
Quarto theme.}
\item{\code{example.qmd}}{Sample HTML report (if \code{examples = TRUE}).}
\item{\code{slides.qmd}}{Sample revealjs presentation (if \code{examples = TRUE}).}
}

In your \code{.qmd} YAML header, reference the SCSS like this:

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{format:
  html:
    theme: [brand, brandkit.scss]
}\if{html}{\out{</div>}}

For ggplot2 theming, just add \code{library(brandkit)} in a setup chunk —
the auto-applied theme and scales handle the rest.
}
