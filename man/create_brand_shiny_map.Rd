% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brand_shiny.R
\name{create_brand_shiny_map}
\alias{create_brand_shiny_map}
\title{Set Up a Branded Shiny Map App}
\usage{
create_brand_shiny_map(path = ".", overwrite = FALSE)
}
\arguments{
\item{path}{Project directory. Defaults to the current working
directory. Created if it doesn't exist.}

\item{overwrite}{Logical. Overwrite existing files? Default \code{FALSE}.}
}
\value{
Invisibly returns a character vector of copied file paths.
}
\description{
Scaffolds a single-file geospatial app built on \code{\link[=brand_page_fluid]{brand_page_fluid()}}:
a leaflet map with brand-coloured markers, filter controls, a
histogram, and an interactive plotly scatter.
}
\details{
Copies \verb{_brand.yml} (bslib format), \code{app.R}, and any local font and
logo files, exactly as \code{\link[=create_brand_shiny_app]{create_brand_shiny_app()}} does — see its
documentation for how the brand file is written and converted.

Leaflet and plotly both render outside Bootstrap's reach, so neither
picks up the theme on its own. The template shows the two helpers that
bridge that gap: \code{\link[=brand_pal_seq]{brand_pal_seq()}} supplying the colour ramp to
\code{leaflet::colorNumeric()}, and \code{\link[=brand_plotly]{brand_plotly()}} re-applying brand
colours and fonts after \code{ggplotly()} strips them. Both take a \code{mode}
argument, wired here to \code{\link[=brand_dark_mode]{brand_dark_mode()}} so they follow the
dark-mode toggle; the map's tile layer switches with it too.

The template needs the \pkg{leaflet} and \pkg{plotly} packages. Both
are optional for brandkit, so this function reports any that are
missing rather than leaving you to hit the error at app start.
}
\examples{
\dontrun{
create_brand_shiny_map(path = "my-map-app")
shiny::runApp("my-map-app")
}

}
