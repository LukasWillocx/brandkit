---
title: "Brand Report"
subtitle: "A polished starting point for branded HTML reports"
author: "Your Name"
date: today
format:
  html:
    title-block-style: none
    # The nested light:/dark: form is what gives the reader a working
    # light/dark toggle: Quarto emits a Bootstrap stylesheet per mode and
    # the toggle swaps between them. The flat `theme: [brand, brandkit.scss]`
    # form looks equivalent and still shows a toggle, but only the syntax
    # highlighting actually switches — the Bootstrap layer stays light.
    # Both lists are identical; _brand.yml's own color:/color-dark: entries
    # are what differ per mode.
    theme:
      light: [brand, brandkit.scss]
      dark: [brand, brandkit.scss]
    toc: false
---

::: {.brand-banner}
::: {.brand-banner-inner}
::: {.brand-banner-content}
{{< brand logo medium >}}

::: {.brand-banner-title}
{{< meta title >}}
:::

::: {.brand-banner-subtitle}
{{< meta subtitle >}}
:::

::: {.brand-banner-meta}
{{< meta author >}} · {{< meta date >}}
:::
:::
:::
:::

```{r}
#| label: setup
#| include: false
library(brandkit)
library(ggplot2)
library(plotly)
brand_quarto_setup()
```

## Overview

This report was scaffolded by `create_brand_quarto_html()`. It inherits
all styling from `_brand.yml` automatically — Quarto handles the
document theme, brandkit handles ggplot2 and plotly.

## Branded Plot

```{r}
#| label: scatter
#| fig-cap: "Auto-themed via theme_brand() — no manual styling needed"
p <- ggplot(mtcars, aes(mpg, wt, color = factor(cyl))) +
  geom_point(size = 3) +
  labs(title = "MPG vs Weight", color = "Cylinders")
p
```

## Interactive Version

The same plot, made interactive with one extra function call:

```{r}
#| label: plotly-scatter
brand_plotly(p, tooltip = c("x", "y", "colour"))
```

## Branded Table

```{r}
#| label: table
knitr::kable(head(mtcars[, 1:6], 8))
```

## Brand Palette

```{r}
#| label: palette
#| fig-cap: "Discrete brand palette"
#| fig-height: 2
cols <- brand_pal_discrete()
barplot(rep(1, length(cols)), col = cols, border = NA, axes = FALSE)
```

::: {.brand-footer}
{{< meta title >}}

Made with [brandkit](https://github.com/LukasWillocx/brandkit)
:::
